{
  "name": "yts.to",
  "version": "0.1.0",
  "description": "Wrapper for the yts.to API",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rm -rf lib",
    "build": "npm run clean && ./node_modules/.bin/babel src --out-dir lib",
    "test": "npm run build && node ./tests/index.js"
  },
  "keywords": [
    "yts",
    "yts.to"
  ],
  "author": "Chris Landa <stylesuxx@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "babel": "^5.8.23",
    "request-promise": "^0.4.3",
    "test": "^0.6.0"
  },
  "devDependencies": {
    "tape": "^4.2.1"
  }
}
